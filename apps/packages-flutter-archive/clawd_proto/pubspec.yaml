name: clawd_proto
description: ClawDE RPC protocol types — Session, Message, RepoStatus, ToolCall. Pure Dart, no Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies: {}

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
