name: Multi-Arch Check

# Asserts multi-architecture build compatibility.
# Repo type: Rust + Tauri 2/React + React Native/Expo (nself-org/clawde)
# Flutter removed 2026-06-30 — migrating to RN/Tauri per ASI Policy 2.
# Authority: S30-T05, STORM-CROSS-PLATFORM
# Template: .claude/docs/doctrines/multi-arch-check.workflow.yml

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  multi-arch-stub:
    name: Multi-arch check (N/A)
    runs-on: ubuntu-latest

    steps:
      - name: Report repo type
        run: |
          echo "Repo type: Rust + Tauri 2/React + React Native/Expo (nself-org/clawde)"
          echo "No Go binaries or Docker images produced. Multi-arch check N/A. PASS."

