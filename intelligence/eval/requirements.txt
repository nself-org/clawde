# Ragas evaluation harness — pinned exactly per LEDGER (do not float).
# The Go side (internal/eval/ragas.go) computes the deterministic composite;
# this harness is the canonical producer of the four Ragas metrics.
ragas==0.1.21
datasets>=2.14,<3
langchain>=0.1,<0.2
